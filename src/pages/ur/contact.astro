---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import contactData from '../../content/ur/contact.json';

const { title, urduTitle, description, urduDescription, addressLabel, urduAddressLabel, address, emailLabel, urduEmailLabel, email, websiteLabel, urduWebsiteLabel, website, hoursLabel, urduHoursLabel, hours, urduHours, fridayClosed, urduFridayClosed, phonesLabel, urduPhonesLabel, phones, mapLink, whatsappTitle, urduWhatsappTitle, whatsappText, urduWhatsappText, whatsappButton, urduWhatsappButton, whatsappNumber } = contactData;
---

<Layout 
  title="ہم سے رابطہ کریں - پیام سیرت ٹرسٹ"
  description="گولکنڈہ، حیدرآباد میں پیام سیرت ٹرسٹ سے رابطہ کریں۔ فون، واٹس ایپ، اور ای میل رابطہ کی معلومات۔"
  lang="ur"
>
  <Header lang="ur" currentPage="contact" />
  
  <main>
    <!-- Hero Section -->
    <section class="bg-primary text-white py-16 lg:py-24 geo-pattern">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
          <h1 class="text-4xl lg:text-5xl font-bold mb-4 font-urdu">{urduTitle}</h1>
          <p class="text-2xl text-gold mb-4">{title}</p>
          <p class="text-lg text-white/90 max-w-2xl mx-auto font-urdu leading-loose">{urduDescription}</p>
          <p class="text-white/80 mt-2">{description}</p>
        </div>
      </div>
    </section>

    <!-- Contact Info -->
    <section class="py-16 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid md:grid-cols-2 gap-12" dir="rtl">
          <!-- Contact Details -->
          <div class="space-y-6 text-right">
            <div class="bg-cream rounded-xl p-6">
              <h3 class="font-semibold text-primary mb-1 font-urdu">{urduAddressLabel}</h3>
              <p class="text-sm text-primary mb-2">{addressLabel}</p>
              <p class="text-gray-700 font-urdu">{address}</p>
            </div>
            
            <div class="bg-cream rounded-xl p-6">
              <h3 class="font-semibold text-primary mb-1 font-urdu">{urduEmailLabel}</h3>
              <p class="text-sm text-primary mb-2">{emailLabel}</p>
              <a href={`mailto:${email}`} class="text-primary hover:underline">{email}</a>
            </div>
            
            <div class="bg-cream rounded-xl p-6">
              <h3 class="font-semibold text-primary mb-1 font-urdu">{urduWebsiteLabel}</h3>
              <p class="text-sm text-primary mb-2">{websiteLabel}</p>
              <a href={`https://${website}`} class="text-primary hover:underline">{website}</a>
            </div>
            
            <div class="bg-cream rounded-xl p-6">
              <h3 class="font-semibold text-primary mb-1 font-urdu">{urduHoursLabel}</h3>
              <p class="text-sm text-primary mb-2">{hoursLabel}</p>
              <p class="text-gray-700 font-urdu">{urduHours}</p>
              <p class="text-gray-500 mt-1">{hours}</p>
              <p class="text-red-600 text-sm mt-2 font-urdu">{urduFridayClosed}</p>
              <p class="text-red-500 text-sm">{fridayClosed}</p>
            </div>
          </div>
          
          <!-- Map and Phones -->
          <div class="space-y-6">
            <!-- Map -->
            <div class="bg-cream rounded-xl p-6">
              <div class="bg-gray-200 rounded-lg h-64 flex items-center justify-center mb-4">
                <img src="/map.jpg" alt="Location Map" class="w-full h-full object-cover rounded-lg" onerror="this.style.display='none'; this.parentElement.innerHTML='<span class=\\'text-gray-400 font-urdu\\'>نقشہ پلیس ہولڈر</span>';" />
              </div>
              <a 
                href={mapLink}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-block w-full text-center px-6 py-3 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors font-urdu"
              >
                گوگل میپس میں کھولیں
              </a>
            </div>
            
            <!-- Phone Numbers -->
            <div class="bg-cream rounded-xl p-6" dir="rtl">
              <h3 class="font-semibold text-primary mb-1 font-urdu">{urduPhonesLabel}</h3>
              <p class="text-sm text-primary mb-4">{phonesLabel}</p>
              <div class="space-y-3 text-right">
                {phones.map(phone => (
                  <a 
                    href={`tel:${phone.number.replace(/\s/g, '')}`}
                    class="flex justify-between items-center bg-white rounded-lg p-3 hover:shadow-sm transition-shadow"
                  >
                    <span class="font-medium text-primary">{phone.number}</span>
                    <span class="text-gray-500 text-sm font-urdu">{phone.label}</span>
                  </a>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- WhatsApp Section -->
    <section class="py-16 bg-cream">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="bg-gradient-to-br from-green-600 to-green-700 rounded-2xl p-8 md:p-12 text-white text-center">
          <div class="text-5xl mb-4">💬</div>
          <h2 class="text-3xl font-bold mb-2 font-urdu">{urduWhatsappTitle}</h2>
          <p class="text-xl mb-4 text-green-200">{whatsappTitle}</p>
          <p class="text-white/90 mb-4 font-urdu">{urduWhatsappText}</p>
          <p class="text-white/80 mb-8">{whatsappText}</p>
          <a 
            href={`https://wa.me/${whatsappNumber}`}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block px-8 py-4 bg-white text-green-700 font-semibold rounded-lg hover:bg-green-50 transition-colors shadow-lg font-urdu"
          >
            {urduWhatsappButton}
          </a>
        </div>
      </div>
    </section>
  </main>
  
  <Footer lang="ur" />
</Layout>
